# Python dependencies for EmbeddingGemma-300M with PyO3 integration
# Updated: 2025-09-06 for production deployment

# Core ML libraries for EmbeddingGemma
sentence-transformers>=3.2.0
transformers>=4.49.0
torch>=2.0.0,<2.5.0
numpy>=1.24.0,<2.0.0

# HuggingFace ecosystem
huggingface-hub>=0.19.0
tokenizers>=0.19.0
safetensors>=0.4.0

# For GGUF model support (if needed later)
# gguf>=0.6.0

# Optional: Optimization libraries
# accelerate>=0.25.0  # GPU acceleration
# bitsandbytes>=0.41.0  # Quantization support

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# For memory profiling and debugging
# memory-profiler>=0.61.0
# psutil>=5.9.0

# Logging and monitoring
# wandb>=0.16.0  # Optional: experiment tracking